// This file is intentionally kept as a no-op to avoid Expo Router route warnings.
// The real notifications helper has been moved to app/_utils/notifications.ts
// so it isn't treated as a route by expo-router.

export default function NotificationsRoutePlaceholder() { return null; }
